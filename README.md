# library-app
A microservice web application built with Java 21 and Spring Boot 3.5.2. Features basic CRUD operations, backed by MySQL databases initialized with liquibase, service registration/discovery via Netflix Eureka, event-driven communication via Spring Events, and Docker Compose containerization.
